import type { ReactNode } from 'react'

export default function VideoStudioPage({ children }: { children: ReactNode }) {
  return <section className="route-page route-page-video">{children}</section>
}
