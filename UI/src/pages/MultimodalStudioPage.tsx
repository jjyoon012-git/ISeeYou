import type { ReactNode } from 'react'

export default function MultimodalStudioPage({ children }: { children: ReactNode }) {
  return <section className="route-page route-page-multimodal">{children}</section>
}
