# 이 파일은 데모 실행에 필요한 모든 설정값을 담당합니다

# ── 경로 설정 ─────────────────────────────────────────────
MODEL_PATH = "weights/best.pt"       # 학습된 가중치 파일 경로 (수정 필요)
IMAGE_PATH = "samples/test.jpg"      # 추론할 이미지 경로 (수정 필요)

# ── 모델/이미지 설정 ──────────────────────────────────────
IMG_SIZE    = 224            # 모델 입력 이미지 크기
FACE_MARGIN = 0.25           # 얼굴 크롭 시 여백 비율

# ── ImageNet 정규화 통계 ──────────────────────────────────
IMAGENET_MEAN = [0.485, 0.456, 0.406]
IMAGENET_STD  = [0.229, 0.224, 0.225]
